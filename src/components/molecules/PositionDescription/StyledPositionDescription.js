import styled from "styled-components";

const StyledPositionDescription = styled.div`
  margin: 10px 0;
  padding: 10px 0;
  max-width: 450px;

  p {
    overflow: hidden;
    white-space: nowrap;
    margin: 15px 0;
  }
`;

export default StyledPositionDescription;
