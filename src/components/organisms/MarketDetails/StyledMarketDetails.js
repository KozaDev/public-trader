import styled from "styled-components";
import { StyledCard } from "styles/components";

const StyledMarketDetails = styled(StyledCard)`
  padding: 20px;
  margin: 20px 0;
`;

export default StyledMarketDetails;
